---
title: "Analysis for ERIC SN paper"
date: "`r Sys.Date()`"
output: 
  pdf_document:
    toc: true
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE,  message=FALSE, warning=FALSE, fig.retina = 4)
```

```{r}
# Libraries
pacman::p_load(tidyverse, janitor)

# Parameters
df <- read_csv(here::here("data", "all.csv")) %>% clean_names() %>% remove_empty()

summary(df)

df %>%
  ggplot(aes(eric_date_added)) +
  geom_bar()
```



```{r}
df2 %>% filter(eric_peer_reviewed == "T")

df2 %>% 
  inner_join(df, by = "authors") %>% 
  count(authors) %>% arrange(-n)

df %>% 
  filter(str_detect(dc_type %>% str_to_lower(), "journal")) %>% 
  count(dc_source) %>% arrange(-n)
```

